name: RDP Ngrok Gratis

on:  
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    steps:
    - name: Mengaktifkan RDP Windows
      run: |
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0
        Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
    - name: Download & Setup Ngrok Tunnel
      run: |
        Invoke-WebRequest -Uri "https://equinox.io" -OutFile "ngrok.zip"
        Expand-Archive -Path "ngrok.zip" -DestinationPath "."
    - name: Jalankan Koneksi RDP
      run: |
        net user youtubeuser YoutubeRDP123! /add
        net localgroup administrators youtubeuser /add
        ./ngrok tcp --authtoken 3G3zbrcEJivGX7124xlFdBRf1Vz_5w5WaAR7NZeV6bDkk8te2
